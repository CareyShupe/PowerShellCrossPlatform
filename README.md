# PowerShell Cross-Platform

PowerShell (cross-platform): Works on Windows and Linux (PowerShell 7+).

This repository contains PowerShell profile scripts and VSCode workspace settings intended to work across Windows and Linux. The one-line above documents verified cross-platform compatibility for quick reference.
